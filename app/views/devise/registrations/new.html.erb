<div class="well well">
  <h2>5 Commandments To Always Follow Here</h2>
  <h4><span class="glyphicon glyphicon-hand-right"> Thou shalt have not post confidential stuff here.</span></h4>
  <h4><span class="glyphicon glyphicon-hand-right"> Respect thy team and thy colleagues, that thou shall not post personal attacks.</span></h4>
  <h4><span class="glyphicon glyphicon-hand-right"> Thou shalt not spend more than 1 hour in a day here.</span></h4>
  <h4><span class="glyphicon glyphicon-hand-right"> Thou shalt not post stuff that is offensive.</span></h4>
  <h4><span class="glyphicon glyphicon-hand-right"> Thou shalt blame Felix for broken builds and Prinslow for no snacks in the kitchen at all time.</span></h4>
</div>
<div class="panel panel-default">
  <div class="panel-heading">
    <h1>Sign up</h1>
  </div>

  <div class="panel-body">
    <%= form_for(resource, :as => resource_name, :url => registration_path(resource_name)) do |f| %>
      <%= devise_error_messages! %>
      
      <div class="form-group">
       <%= f.label :name %>
       <%= f.text_field :name, class: "form-control", :autofocus => true %>
      </div>

      <div class="form-group">
        <%= f.label :email %>
        <%= f.email_field :email, class: "form-control" %>
      </div>

      <div class="form-group">
        <%= f.label :password %>
        <%= f.password_field :password, class: "form-control" %>
      </div>

      <div class="form-group">
        <%= f.submit "Sign up", class: "btn btn-primary" %>
      </div>
    <% end %>
  </div>

  <div class="panel-footer">
    <%= render "devise/shared/links" %>
  </div>
</div>